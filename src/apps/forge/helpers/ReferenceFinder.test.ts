// Tests for ReferenceFinder are located in src/tests/ReferenceFinder.test.ts
// This file remains as a placeholder so Jest still discovers a local test module.
describe('ReferenceFinder', () => {
  it('defers real coverage to src/tests/ReferenceFinder.test.ts', () => {
    expect(true).toBe(true);
  });
});
