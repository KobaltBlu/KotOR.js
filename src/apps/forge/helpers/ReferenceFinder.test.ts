// Tests for ReferenceFinder are located in src/tests/ReferenceFinder.test.ts
// This file exists so the helper is discovered; one placeholder test satisfies Jest.
import { test, expect } from '@jest/globals';

test('ReferenceFinder tests live in src/tests/ReferenceFinder.test.ts', () => {
  expect(true).toBe(true);
});
