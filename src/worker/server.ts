/**
 * Server Worker
 * This is a stub for the eventual server worker that will be used to handle the game server logic.
 */

onmessage = function (e: any = {}){
  // console.log('server worker message', e);
}

class OdysseyServer {
  
}