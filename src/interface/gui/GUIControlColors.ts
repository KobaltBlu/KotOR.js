import * as THREE from "three";

export interface GUIControlColors {
    BORDER: THREE.Color;
    BORDER_HOVER: THREE.Color;

    BORDER_HIGHLIGHT: THREE.Color;
    BORDER_HIGHLIGHT_HOVER: THREE.Color;

    TEXT: THREE.Color;
    TEXT_HIGHLIGHT: THREE.Color;
}